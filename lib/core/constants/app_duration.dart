class AppDuration {
  AppDuration._();

  static const mockFast = Duration(milliseconds: 100);
  static const mockNormal = Duration(milliseconds: 300);
  static const mockSlow = Duration(milliseconds: 500);
  static const mockSync = Duration(seconds: 1);

  static const animationFast = Duration(milliseconds: 150);
  static const animationNormal = Duration(milliseconds: 200);
  static const animationSlow = Duration(milliseconds: 300);
}
